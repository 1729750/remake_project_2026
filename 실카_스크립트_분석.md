# 카드 배틀러 프로토타입 — C# 스크립트 분석 문서

Assets/scripts 폴더 전체 스크립트 기능 분석

작성일: 2026-07-24    |    분석 대상: Assets/scripts 하위 .cs 파일 30개 (Editor·씬 파일 제외)

# 1. 개요

이 문서는 Unity 프로젝트 Assets/scripts 폴더 아래의 모든 C# 스크립트를 폴더 구조별로 정리하고, 스크립트별 주요 함수와 기능을 요약한 것이다. 코드 자체(정적 분석)만을 근거로 작성했으며, 씬 파일(SampleScene.unity)의 컴포넌트 배치나 인스펙터 값은 참고하지 않았다.

## 폴더 구조

```
Assets/scripts/
├─ Battles/          전투(턴, 큐, 손패, 캐릭터 상태, 보상) 관련 스크립트
├─ Cards/            카드 정의·인스턴스·효과·시각화 스크립트
│  └─ effects/       Effect 계열 런타임 서브클래스
├─ HyeGyoServer/      네트워크(멀티플레이) 관련 스크립트 (미완성)
├─ GameManager.cs     씬 진입점 / 게임 상태 머신
├─ InputManager.cs    Input System 스택 기반 컨텍스트 관리자
├─ PlayerInputManager.cs  InputManager 싱글턴 서브클래스
├─ MapManager.cs / MapVisual.cs   적 선택 화면
└─ PlayerManager.cs   플레이어 영구 상태(체력, 덱) 보관
```

## 전체 구조 요약

GameManager(게임 상태 전환) → BattleManager(전투 상태 머신, TurnManager 소유) → CharacterManager(플레이어/적 공용, HandManager+QueueManager 소유) → CardInstance/CardDefinition(카드 데이터) → CardEffect/Effect 계열(효과 계산) 순으로 계층을 이루며, MapManager/RewardManager가 전투 전후 화면을 담당한다.

# 2. 폴더별 스크립트 상세 분석

# 2.1 Battles/

## BattleManager.cs

`경로: Assets/scripts/Battles/BattleManager.cs`

**역할:** 전투 전체를 관장하는 MonoBehaviour 싱글턴(Instance). 전투 상태 머신(BattleState: BattleStarting→TurnStart→Turn→TurnEnd→BattleFinish)을 갖고, TurnManager를 생성/소유하며 턴 시작/종료 이벤트를 구독한다. 다른 클래스들이 상대방 조회, 이모지 조회, 패배 통지 등을 위해 거쳐가는 허브 역할.

**주요 함수 / 멤버:**

| **함수/멤버** | **기능 설명** |
| --- | --- |
| `Awake()` | 싱글턴 인스턴스 설정(중복 시 자기 자신 파괴) |
| `Init()` | 양쪽 CharacterManager 초기화, TurnTimerOverlay GameObject 생성, TurnManager 생성 및 이벤트 구독 |
| `StartBattle(playerData, enemyData)` | CharacterData의 덱을 런타임 카드 리스트로 풀어(UnpackCardCollection) 양쪽 CharacterManager를 초기화하고 BattleStarting 상태로 전환 |
| `UnpackCardCollection(collection)` | CardCollection 에셋의 카드들을 Instantiate로 깊은 복사해 런타임 List<CardDefinition>으로 반환 (원본 에셋 오염 방지, static) |
| `Tick(deltaTime)` | BattleStarting 상태면 시작 딜레이 카운트다운, Turn 상태면 TurnManager.Tick 호출 |
| `OnBattleStarted()` | 시작 딜레이 종료 시 첫 턴 시작 |
| `OnTurnStarted()` | TurnStart 상태 전환 후 양쪽 CharacterManager.OnTurnStart() 호출, Turn 상태로 전환 |
| `OnTurnEnded()` | TurnEnd 상태 전환 후 양쪽 CharacterManager.OnTurnEnd() 호출, 전투가 안 끝났으면 다음 턴 시작 |
| `NotifyDefeat(loser)` | 패배자가 플레이어면 Application.Quit(), 적이면 GameManager.EndBattle() 호출하며 BattleFinish로 전환 |
| `GetOpponent(user)` | 주어진 CharacterManager의 상대편을 반환 |
| `GetEmoji(effectType)` | (static) EffectType별 표시용 이모지 매핑(🗡️🛡️⚙️✊💔🥀🧘) |
| `Update()` | GameManager가 Battle 상태일 때만 Tick(Time.deltaTime) 호출 |

## CharacterManager.cs

`경로: Assets/scripts/Battles/CharacterManager.cs`

**역할:** 플레이어와 적 모두에 붙는 공용 MonoBehaviour. playerControlled 플래그로 Input System 입력 처리와 AI(SelectRandomCard) 로직을 구분한다. HP/방어도/코스트/효과 리스트/덱을 보유하며 HandManager, QueueManager를 각각 하나씩 소유한다. 효과 적용(HP/방어 연산)이 이뤄지는 유일한 지점.

**주요 함수 / 멤버:**

| **함수/멤버** | **기능 설명** |
| --- | --- |
| `CharacterInit(deck, maxHealth)` | 전투 시작 시 덱 생성(CardInstance로 래핑) → 셔플 → 손패 채우기 |
| `ShuffleDeck()` | Fisher-Yates 방식으로 _deck 셔플 |
| `Update()` | 매 프레임 코스트 표시 갱신 |
| `OnTurnStart()` | 코스트 +1(MaxCost=10 상한), 보유 효과들의 OnTurnStarted 호출, 코스트/효과 표시 갱신, 손패 리필 |
| `OnTurnEnd()` | 큐 틱(QueueManager.TickQueueCards) → 효과들의 OnTurnEnded 호출 → AI면 랜덤 카드 선택 → 특수 행동(Redraw/Guard) 또는 선택된 카드 사용 처리 |
| `Redraw()` | 손패 전체를 덱에 되돌리고 셔플 후 재충전 (특수 행동) |
| `GainGuard()` | 이미 Guard 효과가 없으면 지속시간 1의 Guard 효과 부여 (특수 행동: 방어) |
| `SelectCard(index)` | Turn 상태에서만 동작. RedrawAction/DefenseAction 특수 인덱스 처리 또는 일반 카드 선택 |
| `SelectRandomCard()` | AI 전용: 현재 코스트로 낼 수 있는 카드 중 무작위 하나 선택 |
| `Init() / Clear()` | HandManager/QueueManager 생성 및 HP/방어/코스트/효과 초기화 |
| `TakeDamage(amount) / Attacked(amount)` | 방어도부터 깎고 초과분을 체력에서 차감, 체력 0 이하 시 BattleManager.NotifyDefeat 호출 |
| `Heal(amount) / AddDefense(amount)` | 체력 회복 / 방어도 증가(HP바·방어 표시 갱신) |
| `PlayCard(card)` | CardInstance.Play(this) 호출 (큐에서 쿨다운 만료 시 QueueManager가 호출) |
| `QueueCard(card, obj) / DrawCard() / ReturnToDeck(card)` | 큐에 카드 등록 / 덱 최상단 카드 뽑기 / 카드를 덱으로 반환 |
| `RemoveEffect<T>()` | 지정 타입의 효과를 찾아 OnExpired 호출 후 제거 |
| `ApplyEffect(cardEffect)` | 대상 자신의 효과들의 OnApplied 실행 → EffectType이 Attack/Defend면 즉시 데미지/방어 처리, 그 외 타입은 기존 동일 타입 효과에 누적(AddMagnitude) 또는 신규 생성 |
| `GetEffectPrioritize()` | 효과 리스트를 EffectPriority 내림차순 정렬해 반환 (OnApplying 적용 순서 결정용) |
| `UpdateCostDisplay()/UpdateDefenseDisplay()/UpdateEffectList()` | 코스트/방어도/효과 목록 TextMeshPro UI 갱신 |

## TurnManager.cs

`경로: Assets/scripts/Battles/TurnManager.cs`

**역할:** MonoBehaviour가 아닌 순수 C# 클래스. BattleManager가 생성/틱하며, 턴 시작/종료 시 OnTurnStarted/OnTurnEnded 이벤트를 발행하고 TurnTimerOverlay(파이 모양 타이머)와 턴 번호 TextMeshPro를 갱신한다.

**주요 함수 / 멤버:**

| **함수/멤버** | **기능 설명** |
| --- | --- |
| `생성자(turnDuration, overlay, turnText)` | 턴 제한시간, 타이머 오버레이, 턴 번호 텍스트 주입 |
| `StartTurn()` | 턴 활성화, 턴 카운트 증가, 경과시간 초기화, OnTurnStarted 이벤트 발행 |
| `EndTurn()` | 턴 비활성화, 경과시간 초기화, OnTurnEnded 이벤트 발행 |
| `Reset()` | 턴 상태 전체 초기화(카운트 0, 오버레이 0) |
| `Tick(deltaTime)` | 경과시간 누적, 오버레이 채움 비율 갱신, 제한시간 초과 시 EndTurn 자동 호출 |
| `GetElapsedRatio()` | 경과시간/턴제한시간 비율(0~1) 반환 — 오버레이 표시에 사용 |

## TurnTimerOverlay.cs

`경로: Assets/scripts/Battles/TurnTimerOverlay.cs`

**역할:** 남은 턴 시간을 파이 슬라이스(부채꼴) 모양의 절차적 메시로 시각화하는 MonoBehaviour. MeshFilter/MeshRenderer 필요.

**주요 함수 / 멤버:**

| **함수/멤버** | **기능 설명** |
| --- | --- |
| `Awake()` | Mesh 생성 및 MeshFilter에 할당, "Sprites/Default" 셰이더로 반투명 검은색 머티리얼 설정 |
| `SetFill(ratio)` | 채움 비율(0~1)을 받아 변화가 있을 때만 BuildMesh 재호출 (동일 값이면 스킵) |
| `BuildMesh(ratio)` | 중심점 + 원호를 따라 segments개 정점을 생성해 부채꼴 삼각형 팬(triangle fan) 메시를 만듦. 90도(위쪽)에서 시작해 시계방향으로 ratio*360도만큼 채움 |

## HandManager.cs

`경로: Assets/scripts/Battles/HandManager.cs`

**역할:** CharacterManager가 소유하는 순수 C# 클래스. 고정 4칸 손패 배열과 각 칸의 CardVisual GameObject를 관리하며 선택 상태를 추적한다.

**주요 함수 / 멤버:**

| **함수/멤버** | **기능 설명** |
| --- | --- |
| `생성자(characterManager, slotsRoot, isHandVisualized)` | slotsRoot의 자식들을 슬롯으로 등록, Resources에서 Card 프리팹 로드 |
| `FillHand()` | 빈 손패 칸마다 소유자 덱에서 카드를 뽑아(DrawCard) CreateCardVisual로 시각화 후 선택 표시 갱신 |
| `CreateCardVisual(i)` | 카드 프리팹을 슬롯 자식으로 인스턴스화하고 CardVisual.SetCard 호출 |
| `ReturnHandToDeck()` | 손패 전체를 소유자 덱으로 반환하고 시각 오브젝트 파괴 (Redraw용) |
| `SelectCard(index) / UnselectCard()` | 선택 인덱스 설정/해제 후 하이라이트 갱신 |
| `UseCard()` | 선택된 카드를 Use() 처리하고 QueueManager에 등록 시도, 성공하면 손패에서 제거 |
| `RefreshSelection()` | 각 카드 시각 오브젝트의 SetSelected를 현재 선택 인덱스와 비교해 갱신 |

## QueueManager.cs

`경로: Assets/scripts/Battles/QueueManager.cs`

**역할:** CharacterManager가 소유하는 순수 C# 클래스. 고정 3칸 대기열(쿨다운 중인 카드)을 관리하며, 삽입 시 쿨다운이 짧은 순으로 정렬(삽입 정렬) 유지한다.

**주요 함수 / 멤버:**

| **함수/멤버** | **기능 설명** |
| --- | --- |
| `생성자(slotsRoot)` | slotsRoot의 자식들을 큐 슬롯으로 등록 |
| `AddCard(card, cardObject)` | 빈 칸에 카드 삽입 후 쿨다운 오름차순이 되도록 인접 교환(삽입 정렬), 카드를 앞면으로 전환, 슬롯 위치로 이동 |
| `MoveVisualsToSlots()` | 큐 내 각 카드 오브젝트를 대응하는 슬롯으로 이동(CardVisual.MoveTo 또는 직접 위치 지정) |
| `TickQueueCards(characterManager)` | 큐의 모든 카드 쿨다운 감소, 준비된(IsReady) 카드는 CharacterManager.PlayCard 호출 후 큐에서 제거·시각 오브젝트 파괴 |
| `CompactQueue()` | 틱 후 생긴 빈 칸을 앞으로 당겨 채움(압축) |
| `GetQueue() / GetSlot(index)` | 현재 큐 배열 / 지정 인덱스의 슬롯 Transform 반환 |

## CharacterData.cs

`경로: Assets/scripts/Battles/CharacterData.cs`

**역할:** ScriptableObject. 캐릭터(주로 적)의 최대 체력과 덱(CardCollection)을 담는 데이터 컨테이너.

**주요 함수 / 멤버:**

| **함수/멤버** | **기능 설명** |
| --- | --- |
| `GetMaxHealth() / GetDeck()` | 최대체력 / 덱(CardCollection) 접근자 |
| `Create(maxHealth, deck)` | (static) 런타임 상태(예: 플레이어 상태)를 CharacterData 인스턴스로 박싱해서 생성 |

## RewardManager.cs

`경로: Assets/scripts/Battles/RewardManager.cs`

**역할:** MonoBehaviour 싱글턴. 전투 종료 후 보상 화면(카드 획득/삭제/강화 선택 → 실제 보상 카드 선택)을 2단계로 관리한다. RewardDisplay 프리팹 3개, 카드 프리팹 여러 개를 동적으로 생성/파괴한다.

**주요 함수 / 멤버:**

| **함수/멤버** | **기능 설명** |
| --- | --- |
| `ShowRewardDisplay()` | RewardDisplay 프리팹 3개를 인스턴스화해 좌우로 배치(카드 획득/삭제/강화)하고 첫 항목 선택 |
| `MoveRewardDisplaySelection(delta)` | 보상 옵션 선택 인덱스를 순환 이동 |
| `ConfirmRewardSelection()` | 선택된 인덱스(0/1/2)에 따라 GameManager.RewardCard/RewardCardDelete/RewardCardEnhance 호출 |
| `ClearRewardDisplay()` | 생성해둔 RewardDisplay 3개 전부 파괴 |
| `ShowRewardCard(rewardCards)` | 주어진 카드 정의 배열로 실제 획득 가능한 카드 시각 오브젝트들을 생성/배치 |
| `MoveRewardCardSelection(delta) / ConfirmRewardCard()` | 보상 카드 선택 이동 / 확정된 카드 정의 반환 |
| `ClearRewardCard()` | 이전 보상 카드 라운드의 시각 오브젝트 정리 |

## RewardDisplay.cs

`경로: Assets/scripts/Battles/RewardDisplay.cs`

**역할:** RewardDisplay 프리팹 루트에 부착되는 단순 MonoBehaviour. 하이라이트 on/off만 담당.

**주요 함수 / 멤버:**

| **함수/멤버** | **기능 설명** |
| --- | --- |
| `Awake()` | Card/HighLight 자식을 찾아 캐싱 후 비활성화 |
| `SetSelected(selected)` | 하이라이트 오브젝트 활성/비활성 전환 |

# 2.2 Cards/

## CardDefinition.cs

`경로: Assets/scripts/Cards/CardDefinition.cs`

**역할:** ScriptableObject. 카드 한 장의 정적 데이터(쿨다운, 코스트, 효과 리스트, 스프라이트, 카드 타입)를 에디터에서 작성하는 에셋.

**주요 함수 / 멤버:**

| **함수/멤버** | **기능 설명** |
| --- | --- |
| `GetCooldown() / GetCost() / GetCardType() / GetSprite()` | 각 필드 접근자 |
| `GetEffects()` | 이 카드가 가진 CardEffect 배열 반환 |

## CardInstance.cs

`경로: Assets/scripts/Cards/CardInstance.cs`

**역할:** CardDefinition을 실제 플레이 상태(쿨다운 잔여, 사용 여부, 소유자)와 함께 감싸는 런타임 클래스. 덱/손패/큐를 오가는 실제 단위.

**주요 함수 / 멤버:**

| **함수/멤버** | **기능 설명** |
| --- | --- |
| `생성자(definition, owner)` | 정의로부터 쿨다운/코스트/효과 복사 및 초기화 |
| `RefreshInstance()` | 정의의 쿨다운/코스트/효과 리스트를 인스턴스 필드로 복사 |
| `GetCost() / GetCooldownLeft() / IsReady()` | 코스트 / 남은 쿨다운 / 사용 가능 여부(쿨다운 0 이하) |
| `Use()` | 카드 사용 처리: 쿨다운을 정의값으로 리셋하고 played 플래그 설정 |
| `TickCooldown()` | 남은 쿨다운을 1 감소(0 이하로는 내려가지 않음) |
| `Play(characterManager)` | 카드 효과 각각에 대해 Reset → 소유자 효과들의 OnApplying(우선순위 역순) 실행 → 대상 CharacterManager 결정 → ApplyEffect 호출, 종료 후 소유자 덱으로 복귀 |

## CardEffect.cs

`경로: Assets/scripts/Cards/CardEffect.cs`

**역할:** CardDefinition에 담기는 (Effect, EffectTarget) 쌍 + 해석 시 누적되는 multiplier/adder. 카드 한 장의 개별 효과 항목 하나를 표현.

**주요 함수 / 멤버:**

| **함수/멤버** | **기능 설명** |
| --- | --- |
| `생성자(effect, target)` | Effect 원본과 대상(User/Opponent) 지정 |
| `Reset()` | multiplier=1, adder=0으로 초기화 (매 Play마다 새로 계산하기 위함) |
| `Add(i) / Multiply(i)` | 누적 가산 / 누적 곱셈 (OnApplying/OnApplied 훅에서 호출됨) |
| `GetTarget(user)` | _target이 User면 user 자신, Opponent면 BattleManager.GetOpponent(user) 반환 |
| `GetMagnitude()` | (효과 원본 magnitude * multiplier)를 내림한 뒤 adder를 더한 최종 수치 계산 |

## CardType.cs

`경로: Assets/scripts/Cards/CardType.cs`

**역할:** 카드 분류 enum.

**주요 함수 / 멤버:**

| **함수/멤버** | **기능 설명** |
| --- | --- |
| `enum { Attack, Skill }` | 공격 카드 / 스킬 카드 구분 (현재 로직에서 직접 분기에는 사용되지 않고 표시용으로 예약됨) |

## CardCollection.cs

`경로: Assets/scripts/Cards/CardCollection.cs`

**역할:** ScriptableObject. CardDefinition 리스트(덱)를 감싸는 컨테이너.

**주요 함수 / 멤버:**

| **함수/멤버** | **기능 설명** |
| --- | --- |
| `GetCards() / GetCount()` | 카드 배열 / 개수 반환 |
| `Create(cards)` | (static) 런타임 카드 리스트(예: PlayerManager 덱)를 CardCollection 인스턴스로 박싱 |

## CardVisual.cs

`경로: Assets/scripts/Cards/CardVisual.cs`

**역할:** Card 프리팹 루트에 부착. 카드의 시각적 표현(배경, 스프라이트, 효과/코스트/쿨타임 텍스트, 앞뒷면, 선택 하이라이트, 이동 애니메이션)을 전담.

**주요 함수 / 멤버:**

| **함수/멤버** | **기능 설명** |
| --- | --- |
| `Awake()` | 프리팹 자식 컴포넌트(Front/Back, background, sprite, 각종 텍스트, 하이라이트) 참조 캐싱 |
| `SetSelected(selected)` | 선택 하이라이트 표시 전환 |
| `SetFace(front)` | 앞면/뒷면 오브젝트 활성 전환 (큐에 카드가 들어가면 앞면으로 전환됨) |
| `SetCard(cardInstance, showFront)` | 카드 정의로부터 스프라이트/코스트/효과 텍스트(이모지:수치, 줄바꿈 구분)를 채우고 쿨타임 갱신, 앞뒷면 설정 |
| `GetBackgroundSize()` | 배경 스프라이트의 월드 단위 크기 반환 (레이아웃 계산용) |
| `SetSize(targetSize)` | 배경 크기가 목표 크기가 되도록 X/Y 개별 비율로 스케일 조정 |
| `MoveTo(targetPosition, duration)` | 코루틴으로 부드러운 이동 애니메이션 시작(SmoothStep 보간) |
| `MoveRoutine(...)` | 실제 이동 보간 코루틴 |
| `Update()` | 매 프레임 쿨타임 텍스트 갱신 |
| `RefreshCooltime()` | 남은 쿨다운 텍스트("⏱:N") 갱신 |

## DeckDisplay.cs

`경로: Assets/scripts/Cards/DeckDisplay.cs`

**역할:** CardDefinition 리스트를 4열 그리드로 배치해 전체 덱을 보여주는 화면 크기 컨테이너 MonoBehaviour. 스크롤 대신 보이는 행 구간(_topRow)을 옮기는 방식의 페이징을 사용 (덱 카드 삭제 화면에서 사용됨).

**주요 함수 / 멤버:**

| **함수/멤버** | **기능 설명** |
| --- | --- |
| `SetSize(size)` | 레이아웃 계산에 쓰일 컨테이너 크기(월드 단위) 지정, SetDeck 전에 호출 필요 |
| `SetDeck(cards, horizontalPadding, verticalPadding?)` | 카드들을 스폰(SpawnCard)하고 카드 크기/셀 크기/보이는 행 수를 계산해 그리드 배치 |
| `GetSelectedIndex()` | 현재 선택된 카드 인덱스 반환 |
| `MoveSelectionHorizontal(delta)` | 같은 행 내에서 좌우 선택 이동 (행을 넘어가면 무시) |
| `MoveSelectionVertical(delta)` | 위/아래 행으로 선택 이동, 필요 시 보이는 행 구간(_topRow) 스크롤 |
| `SetSelectedIndex(index)` | 이전 선택 해제 후 새 인덱스 하이라이트 |
| `LayoutCards()` | _topRow 기준으로 보이는 카드만 활성화하고 그리드 좌표에 배치 |
| `SpawnCard(def)` | Card 프리팹 인스턴스화 후 소유자 없는 CardInstance로 표시 전용 래핑 |
| `ClearCards()` | 기존 카드 시각 오브젝트 전부 파괴 |

## 2.2.1 Cards/effects/ (Effect 계열)

## Effect.cs

`경로: Assets/scripts/Cards/effects/Effect.cs`

**역할:** 모든 런타임 효과의 베이스 클래스. CardEffect 해석 과정에서 호출되는 생명주기 훅들을 가상 메서드로 제공하며, 하위 클래스가 필요한 훅만 오버라이드한다.

**주요 함수 / 멤버:**

| **함수/멤버** | **기능 설명** |
| --- | --- |
| `생성자(effectType, magnitude, effectPriority=0)` | 효과 종류/수치/적용 우선순위 지정 |
| `GetEffectType() / GetMagnitude() / GetEffectPriority()` | 각 필드 접근자 |
| `AddMagnitude(magnitude)` | 기존 효과에 수치 누적 (동일 타입 효과 중첩 시 사용) |
| `Create(type, magnitude)` | (static 팩토리) EffectType에 따라 HardenEffect/StrengthEffect/VulnerableEffect/WeakEffect/GuardEffect 중 알맞은 서브클래스를 생성, 매칭 안 되면 기본 Effect 반환 |
| `OnExpired(subject)` | 효과가 제거될 때 호출 (기본은 아무 동작 없음, virtual) |
| `OnTurnStarted(subject) / OnTurnEnded(subject)` | 턴 시작/종료 시 호출 (virtual, 지속시간 감소 등에 사용) |
| `OnApplying(subject, effect, actualUse)` | 행동 주체 자신의 효과가 발동 전 카드 효과를 수정할 때 호출 (virtual) |
| `OnApplied(subject, effect, actualUse)` | 대상(피격자) 자신의 효과가 받는 효과를 수정할 때 호출 (virtual) |

## EffectType.cs

`경로: Assets/scripts/Cards/effects/EffectType.cs`

**역할:** 효과 종류 enum.

**주요 함수 / 멤버:**

| **함수/멤버** | **기능 설명** |
| --- | --- |
| `enum { Attack, Defend, Weak, Vulnerable, Strength, Harden, Guard }` | 공격/방어(즉시 처리)와 나머지 지속형 상태이상/버프 구분 |

## EffectTarget.cs

`경로: Assets/scripts/Cards/effects/EffectTarget.cs`

**역할:** 효과 대상 enum.

**주요 함수 / 멤버:**

| **함수/멤버** | **기능 설명** |
| --- | --- |
| `enum { User, Opponent }` | 카드 사용자 자신 / 상대방 중 효과가 누구에게 적용되는지 지정 |

## HardenEffect.cs

`경로: Assets/scripts/Cards/effects/HardenEffect.cs`

**역할:** 방어 카드 사용 시 방어도 획득량을 늘려주는 버프 (영구 지속, 우선순위 2).

**주요 함수 / 멤버:**

| **함수/멤버** | **기능 설명** |
| --- | --- |
| `OnApplying(b, effect, a)` | 발동하려는 효과가 Defend 타입이면 magnitude만큼 가산(Add) |

## StrengthEffect.cs

`경로: Assets/scripts/Cards/effects/StrengthEffect.cs`

**역할:** 공격 카드 사용 시 데미지를 늘려주는 버프 (영구 지속, 우선순위 2).

**주요 함수 / 멤버:**

| **함수/멤버** | **기능 설명** |
| --- | --- |
| `OnApplying(b, effect, a)` | 발동하려는 효과가 Attack 타입이면 magnitude만큼 가산(Add) |

## VulnerableEffect.cs

`경로: Assets/scripts/Cards/effects/VulnerableEffect.cs`

**역할:** 적용 대상이 받는 공격 피해를 증폭시키는 디버프. 매 턴 종료 시 magnitude가 1씩 감소하며 0 이하가 되면 자동 제거된다 (우선순위 1).

**주요 함수 / 멤버:**

| **함수/멤버** | **기능 설명** |
| --- | --- |
| `OnApplied(b, effect, a)` | 받는 효과가 Attack 타입이면 1.2배 곱연산(Multiply) |
| `OnTurnEnded(subject)` | magnitude 1 감소, 0 이하면 RemoveEffect<VulnerableEffect>() 호출 |

## WeakEffect.cs

`경로: Assets/scripts/Cards/effects/WeakEffect.cs`

**역할:** 적용 대상이 가하는 공격 피해를 감소시키는 디버프. 매 턴 종료 시 magnitude가 1씩 감소하며 소멸 (우선순위 1).

**주요 함수 / 멤버:**

| **함수/멤버** | **기능 설명** |
| --- | --- |
| `OnApplying(b, effect, a)` | 발동하려는 효과가 Attack 타입이면 0.8배 곱연산(Multiply) |
| `OnTurnEnded(subject)` | magnitude 1 감소, 0 이하면 RemoveEffect<WeakEffect>() 호출 |

## GuardEffect.cs

`경로: Assets/scripts/Cards/effects/GuardEffect.cs`

**역할:** 받는 공격 피해를 절반으로 줄이는 버프. "방어" 특수 행동으로 얻으며 매 턴 시작 시 magnitude가 감소해 1턴만 지속된다 (우선순위 1).

**주요 함수 / 멤버:**

| **함수/멤버** | **기능 설명** |
| --- | --- |
| `OnApplied(b, effect, a)` | 받는 효과가 Attack 타입이면 0.5배 곱연산(Multiply) |
| `OnTurnStarted(subject)` | magnitude 1 감소, 0 이하면 RemoveEffect<GuardEffect>() 호출 |

# 2.3 HyeGyoServer/ (네트워크, 미완성)

## NetworkLauncher.cs

`경로: Assets/scripts/HyeGyoServer/NetworkLauncher.cs`

**역할:** 파일명과 달리 실제로는 Unity 기본 스크립트 템플릿(class NewMonoBehaviourScript)이 그대로 들어있는 빈 뼈대 파일. 아직 네트워크 실행 로직이 작성되지 않은 상태.

**주요 함수 / 멤버:**

| **함수/멤버** | **기능 설명** |
| --- | --- |
| `Start()` | 빈 함수 (미구현) |
| `Update()` | 빈 함수 (미구현) |

## NetworkMatchBridge.cs

`경로: Assets/scripts/HyeGyoServer/NetworkMatchBridge.cs`

**역할:** Unity Netcode(NetworkBehaviour)를 상속하는 브릿지 클래스. GameManager/BattleManager/PlayerManager/PlayerInputManager 참조 필드만 선언되어 있고 실제 로직은 아직 없음 — 향후 멀티플레이 동기화 지점을 위한 자리 표시자.

**주요 함수 / 멤버:**

| **함수/멤버** | **기능 설명** |
| --- | --- |
| `필드만 존재` | gameManager, battleManager, playerManager, playerInputManager 참조 (기능 미구현) |

# 2.4 최상위 스크립트 (Assets/scripts/*.cs)

## GameManager.cs

`경로: Assets/scripts/GameManager.cs`

**역할:** MonoBehaviour 싱글턴, DontDestroyOnLoad. 게임 전체 상태 머신(GameState: StartScreen/Battle/BattleEnd/SelectEnemy)을 관리하며, 상태 전환에 따라 InputManager 컨텍스트를 로드/언로드하고 각 매니저(BattleManager/MapManager/RewardManager)의 활성화 여부를 이 지점에서만 제어한다.

**주요 함수 / 멤버:**

| **함수/멤버** | **기능 설명** |
| --- | --- |
| `Awake()` | 싱글턴 설정, 씬 전환 시 파괴 방지 |
| `Start()` | StartScreen 상태로 시작, battleManager.Init() 호출 후 EndBattle()로 보상 화면 진입 |
| `StartBattle(enemyData)` | Battle 상태로 전환, PlayCard1~4/ReDraw/Defense 입력 바인딩 로드, BattleManager.StartBattle 호출 |
| `PlayCard(index)` | 현재 턴(BattleState.Turn)일 때만 플레이어 CharacterManager.SelectCard 호출하는 공용 가드 |
| `EndBattle()` | BattleEnd 상태로 전환, 입력 언로드, 보상 화면 표시 |
| `ShowRewardDisplay()` | 보상 옵션(획득/삭제/강화) 표시 및 좌우 선택+확정 입력 바인딩 |
| `RewardCard()` | 카드 획득 보상 카드 목록을 표시하고 좌우 선택+확정 입력 바인딩 |
| `ConfirmRewardCard()` | 선택된 카드를 PlayerManager.AddCard로 덱에 추가 후 보상 화면 종료 |
| `RewardCardDelete()` | 화면 크기 DeckDisplay를 띄워 플레이어 덱에서 버릴 카드를 선택하게 함 |
| `DiscardCard(index)` | PlayerManager.DiscardCard 호출 후 보상 화면 종료 |
| `RewardCardEnhance()` | 카드 강화(로직 미구현, 메뉴만 닫고 복귀) |
| `ConfirmReward()` | 보상 화면(어떤 종류든) 종료 공통 지점 → 적 선택 화면으로 이동 |
| `ShowEnemySelection()` | SelectEnemy 상태로 전환, 좌우 선택+확정 입력 바인딩, MapManager에 적 후보 표시 요청 |
| `GetGameState() / SetGameState(newState)` | 현재 상태 조회 / 상태 변경 및 LoadAppropriateManager 호출 |
| `LoadAppropriateManager()` | 현재 GameState에 따라 battleManager/mapManager GameObject를 활성/비활성화, rewardManager는 BattleEnd 상태에서만 활성 (독립적 오버레이) |

## InputManager.cs

`경로: Assets/scripts/InputManager.cs`

**역할:** Input System 액션맵을 스택으로 관리하는 컨텍스트 매니저. Load로 새 입력 컨텍스트를 쌓으면 이전 컨텍스트의 맵은 비활성화되고, Unload로 제거하면 그 아래 깔린 컨텍스트가 다시 활성화된다 (메뉴 위에 메뉴가 뜨는 UI 흐름에 대응).

**주요 함수 / 멤버:**

| **함수/멤버** | **기능 설명** |
| --- | --- |
| `Load(mapName, bindings)` | 지정 액션맵을 찾아 바인딩 딕셔너리와 함께 새 InputContext로 스택에 push, 이전 맵 비활성화 후 새 맵 활성화 |
| `Unload()` | 스택 맨 위 컨텍스트 pop, 맵 비활성화, 남은 스택이 있으면 그 맵 재활성화 |
| `Update()` | 매 프레임 스택 최상단 컨텍스트의 Poll() 호출 |
| `InputContext.Poll()` | (내부 클래스) 등록된 각 액션이 이번 프레임에 트리거됐는지 확인 후 콜백 실행 |

## PlayerInputManager.cs

`경로: Assets/scripts/PlayerInputManager.cs`

**역할:** InputManager를 상속하는 싱글턴 서브클래스. 별도 로직 추가 없이 플레이어 전용 인스턴스 접근점만 제공.

**주요 함수 / 멤버:**

| **함수/멤버** | **기능 설명** |
| --- | --- |
| `Awake()` | 싱글턴 설정, DontDestroyOnLoad 지정 (부모 InputManager 기능은 그대로 상속) |

## PlayerManager.cs

`경로: Assets/scripts/PlayerManager.cs`

**역할:** MonoBehaviour 싱글턴, DontDestroyOnLoad. 전투 사이에도 유지되는 플레이어의 영구 상태(최대 체력, 덱)를 보관하고, 전투 시작 시 CharacterData로 박싱해 넘겨준다.

**주요 함수 / 멤버:**

| **함수/멤버** | **기능 설명** |
| --- | --- |
| `Awake()` | 싱글턴 설정 및 씬 전환 시 유지 |
| `GetCharacterData()` | 현재 최대 체력과 덱을 CharacterData.Create로 박싱해 반환 (BattleManager.StartBattle에 전달됨) |
| `GetDeck() / GetMaxHealth() / GetCharacterManager()` | 각 필드 접근자 |
| `AddCard(card)` | 덱 리스트에 카드 정의 추가 (보상 획득 시 사용) |
| `DiscardCard(index)` | 지정 인덱스의 카드를 덱에서 제거하고 반환 (보상 삭제 시 사용) |

## MapManager.cs

`경로: Assets/scripts/MapManager.cs`

**역할:** MonoBehaviour 싱글턴. GameState.SelectEnemy일 때만 활성화되는 적 선택 화면. CharacterData 후보 3개를 전시하고 좌우 선택 후 확정하면 GameManager.StartBattle을 호출한다. 또한 덱 성향을 분석해 표시용 수치를 계산하는 헬퍼 함수들을 포함한다.

**주요 함수 / 멤버:**

| **함수/멤버** | **기능 설명** |
| --- | --- |
| `Awake()` | 싱글턴 설정, 자식의 MapVisual 컴포넌트들 캐싱 |
| `ShowEnemySelection(candidates)` | 적 후보 데이터를 각 MapVisual에 설정하고 선택 하이라이트 초기화 |
| `MoveSelection(delta)` | 선택 인덱스를 순환 이동 |
| `RefreshSelectionHighlight()` | 각 MapVisual의 SetSelected를 현재 선택 인덱스와 비교해 갱신 |
| `ConfirmSelection()` | 선택된 후보를 전투 기록에 남기고 GameManager.StartBattle 호출 |
| `GetCurrentRound() / GetBattleHistory()` | 현재 라운드 수 / 지금까지 상대한 CharacterData 이력 반환 |
| `GetHealth(data)` | 해당 CharacterData의 최대 체력 반환 |
| `GetMostFrequentEffectType(data)` | 덱의 모든 카드 효과를 훑어 (Attack/Defend 제외) EffectType별 (magnitude 합 × 등장 횟수) 비중을 계산해 내림차순 리스트 반환 — 적의 성향 표시용 |
| `GetAttackWeight(data) / GetDefenseWeight(data)` | Attack/Defend 타입 각각의 (magnitude 합 × 등장 횟수) 비중 계산 |
| `GetEffectWeight(data, targetType)` | 위 두 함수가 공유하는 내부 계산 로직 |

## MapVisual.cs

`경로: Assets/scripts/MapVisual.cs`

**역할:** EnemyDisplay 프리팹 루트에 부착되는 MonoBehaviour. MapManager가 넘겨준 CharacterData를 기반으로 공격/방어 비중 막대(AtkBar/DefBar)와 대표 효과 이모지+체력 텍스트를 갱신한다.

**주요 함수 / 멤버:**

| **함수/멤버** | **기능 설명** |
| --- | --- |
| `Awake()` | AtkBar/DefBar/EffectsText/HighLight 자식 컴포넌트 캐싱, 하이라이트 비활성화 |
| `SetSelected(selected)` | 하이라이트 표시 전환 |
| `SetCharacter(data)` | MapManager의 공격/방어 비중을 받아 두 막대 폭을 좌우 대칭으로 배분, 상위 2개 대표 효과 이모지와 체력을 텍스트로 표시 |

# 3. 전체 스크립트 목록 요약표

| **파일명** | **폴더** | **한 줄 요약** |
| --- | --- | --- |
| `BattleManager.cs` | Battles/ | 전투 상태 머신 허브, MonoBehaviour 싱글턴 |
| `CharacterManager.cs` | Battles/ | 플레이어/적 공용 전투 상태 (HP·코스트·효과·덱) |
| `TurnManager.cs` | Battles/ | 턴 타이머/이벤트 (순수 C#) |
| `TurnTimerOverlay.cs` | Battles/ | 턴 제한시간 파이차트 시각화 |
| `HandManager.cs` | Battles/ | 4칸 손패 관리 (순수 C#) |
| `QueueManager.cs` | Battles/ | 3칸 대기열(쿨다운) 관리 (순수 C#) |
| `CharacterData.cs` | Battles/ | 캐릭터 정적 데이터 ScriptableObject |
| `RewardManager.cs` | Battles/ | 전투 후 보상 화면 (2단계 선택) |
| `RewardDisplay.cs` | Battles/ | 보상 옵션 하이라이트 표시 |
| `CardDefinition.cs` | Cards/ | 카드 정적 데이터 ScriptableObject |
| `CardInstance.cs` | Cards/ | 카드 런타임 인스턴스, 효과 해석 진입점 |
| `CardEffect.cs` | Cards/ | 카드 1장 내 개별 효과 항목 (multiplier/adder) |
| `CardType.cs` | Cards/ | enum: Attack/Skill |
| `CardCollection.cs` | Cards/ | 덱(카드 리스트) ScriptableObject 컨테이너 |
| `CardVisual.cs` | Cards/ | 카드 시각화(스프라이트·텍스트·이동·선택) |
| `DeckDisplay.cs` | Cards/ | 전체 덱 그리드 뷰 (삭제 화면 등) |
| `Effect.cs` | Cards/effects/ | 효과 베이스 클래스 + 팩토리(Create) |
| `EffectType.cs` | Cards/effects/ | enum: 효과 종류 7종 |
| `EffectTarget.cs` | Cards/effects/ | enum: User/Opponent |
| `HardenEffect.cs` | Cards/effects/ | 방어 획득량 증가 버프 |
| `StrengthEffect.cs` | Cards/effects/ | 공격 데미지 증가 버프 |
| `VulnerableEffect.cs` | Cards/effects/ | 받는 피해 증폭 디버프 (감쇠형) |
| `WeakEffect.cs` | Cards/effects/ | 가하는 피해 감소 디버프 (감쇠형) |
| `GuardEffect.cs` | Cards/effects/ | 받는 피해 절반 버프 (1턴 한정) |
| `NetworkLauncher.cs` | HyeGyoServer/ | 미구현 빈 템플릿 (파일명과 클래스명 불일치) |
| `NetworkMatchBridge.cs` | HyeGyoServer/ | Netcode 브릿지 자리 표시자 (필드만 존재) |
| `GameManager.cs` | (최상위) | 게임 상태 머신 및 씬 진입점 |
| `InputManager.cs` | (최상위) | Input System 컨텍스트 스택 매니저 |
| `PlayerInputManager.cs` | (최상위) | InputManager 싱글턴 서브클래스 |
| `PlayerManager.cs` | (최상위) | 플레이어 영구 상태(체력·덱) 저장 |
| `MapManager.cs` | (최상위) | 적 선택 화면 로직 + 덱 성향 분석 |
| `MapVisual.cs` | (최상위) | 적 후보 카드 시각화(막대·이모지) |

**참고:** CLAUDE.md에 따르면 CardPrefabSetup.cs(Assets/Editor)는 이 폴더 범위 밖의 에디터 확장 도구이며, 경로 참조가 옛 Assets/resources/preFabs/Card.prefab 위치를 가리키고 있어 자산 재구조화가 끝나기 전까지 정상 동작하지 않는다는 점도 함께 알아두면 좋다. 이번 문서는 Assets/scripts 폴더만을 대상으로 했다.
