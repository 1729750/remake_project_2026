# 실카 기획서 및 작업로그

*2026/07/24 기준*

# 게임 로직

기존 기획서에서 게임 로직 자체는 크게 변화하지 않았음. 자세한 사항은 같이 첨부된 UML파일을 참고해주세요.

# 구현 구조

각 스크립트 별 스크립트의 함수에 대한 자세한 설명은 첨부한 “실카_스크립트_분석.md”파일 참고. 여기서는 스크립트가 어떻게 연결되는지에 대해 언급할 것임

## 인풋 시스템

현재 로직에서 인풋은 인풋매니저가 총괄함. 인풋 매니저는 인풋 맵을 스택 형식으로 저장하는데, 이 인풋 맵에서는 어떤 입력이 어떤 함수와 매칭되는지를 dictionary형식으로 저장하고 있음. 이 인풋 맵은 게임 상태의 변환에 맞춰 Load함수를 통해 스택에 저장되고, 해당 맵이 depricate되면 unload를 통해 이전 인풋 맵으로 돌아감. 이 방식은 게임 중 메뉴를 열거나, 덱매니저(상세는 후술)등의 팝업을 조작할 때 이전 인풋 맵으로 적절히 돌아갈 수 있음을 보증함.

### 에디터 조작 요소

- **inputActions (Input Action Asset)**: 이 매니저가 관리할 입력 액션 에셋. Load()가 여기서 이름으로 액션맵을 찾아 스택에 쌓기 때문에, 게임에서 쓰는 모든 맵(Battle, Select 등)이 이 에셋 안에 정의되어 있어야 함.

## 플레이어 인풋 매니저

2인 멀티 플레이를 위해 인풋 매니저를 상속받는 별개의 스크립트를 제작하여 사용중. 아마 멀티 플레이에서는 멀티플레이어 인풋 매니저 등의 스크립트를 통해 구현할 필요가 있다고 사료됨.

### 에디터 조작 요소

이 클래스 자체에 추가된 에디터 노출 필드는 없음. 인풋 매니저의 inputActions를 그대로 상속받아 사용함.

## 게임 매니저

게임 매니저는 지금 게임의 상태를 저장하고, 그 상태에 맞는 매니저를 활성화(enable)및 작동시작 명령을 하달함. 현재는 enemySelect와 battle, battleEnd상태가 구현되어 있으며, 각각 mapManager와 battlemanager, RewardPanel을 활성화시킴. 또한, 각 매니저끼리 정보를 전달할 일이 있다면 (rewardPanel의 결과로 플레이어의 덱에 카드를 추가하거나, mapManager에서 선택한 적의 정보를 battlemanager로 전달하거나) gameManager의 함수를 통해 정보 전달 및 함수 호출을 실행함.

### 에디터 조작 요소

- **battleManager**: 전투 상태(Battle/BattleEnd)를 담당할 BattleManager 참조. GameState 전환 시 이 오브젝트의 활성/비활성을 게임 매니저가 직접 제어함.

- **rewardManager**: 보상 화면(RewardPanel)을 담당할 RewardManager 참조. BattleEnd 상태일 때만 켜지며, 다른 매니저들과 배타적이지 않고 위에 얹히는 패널로 동작함.

- **inputManager**: 게임 상태별 입력 맵(Battle/Select 등)을 로드·언로드할 InputManager 참조.

- **mapManager**: 적 선택 화면을 담당할 MapManager 참조. GameState.SelectEnemy일 때 활성화됨.

- **firstEnemyData**: 최초 상대로 지정할 CharacterData 자리. enemySelect를 생략하고 startBattle을 시도하는 디버깅 상태를 위해 존재함

- **rewardCards**: 보상 "카드 추가" 화면에 제시할 카드 3장의 고정 목록. 적 후보 정보가 아직 부족해 임시로 인스펙터에서 직접 지정해둔 상태이며, 추후 전체 카드 풀에서 랜덤으로 뽑는 방식으로 교체 필요.

- **enemyCandidates**: 적 선택 화면에 표시할 적 후보 3명의 고정 목록. rewardCards와 마찬가지로 지금은 인스펙터 고정값이며, 추후 랜덤 추첨으로 교체 필요.

## 맵 매니저

### 활성화 조건

GameState가 EnemySelect일 때 gameManager에서 enable

### 비활성화 조건

MapManager에서 confirmEnemy가 호출되었을 시

### 의도한 동작

맵 매니저는 게임매니저로부터 3명의 적을 받아서 화면에 표시함. 이때, 인풋 매니저에 해당 3명의 적을 선택하기 위한 인풋 맵을 로드함. 적의 정보는 CharacterData를 통해 전달되며, mapManager는 해당 데이터를 분석한 다음 MapVisual을 통해 적의 간략한 정보를 표기해줌. 플레이어가 이 정보를 보고 싸우고 싶은 적을 하나 골라 선택하면, GameManager의 StartBattle을 통해 전투를 시작함.

### 현황

적의 정보가 충분히 제작되지 않았으므로, 현재는 기능 테스트를 위해 GameManager에서 리스트를 외부에서 입력받아 전달하고 있음. 이 부분은 수정이 필요함

적의 정보는 공격카드: 방어카드의 비율과, 가장 많이 등장하는 효과 카드의 이미지를 보여줌. 추후 캐릭터를 그릴 시간이 있다면 적의 이미지 또한 포함될 가능성이 있음

### 에디터 조작 요소

맵 매니저 스크립트 자체에는 에디터에 노출된 필드가 없음. 표시할 적 후보 3명(CharacterData)은 게임 매니저의 enemyCandidates 배열을 그대로 주입받아 쓸 뿐임 (위 게임 매니저 항목 참고).

## 배틀 매니저

### 활성화 조건

GameState가 Battle, BattleEnd일 때 gameManager에서 enable

### 비활성화 조건

RewardManager에서 confirmReward가 호출되었을 시

전투의 시작, 진행, 끝을 담당함. 전투 시작 시 gameManager에서 StartBattle이 호출되어, 3초의 시간 후 바로 게임이 진행됨. 배틀매니저는 턴 매니저를 부속으로 가져 턴의 시간이 흘러가게 하는 함수를 매 Update마다 호출하고, 턴이 진행될 때 각 캐릭터 매니저에서 카드를 사용하고, 효과처리를 진행하고, 다음 카드를 뽑는 함수를 호출하는 역할을 함.

### 에디터 조작 요소

- **playerCharacterManager / enemyCharacterManager**: 전투를 벌일 양측 CharacterManager 참조. 전투 시작 시 각각의 CharacterInit이 호출됨.

- **turnText**: 현재 턴 번호를 표시하는 TextMeshPro. 턴 제한시간을 원형 게이지로 보여주는 오버레이도 이 오브젝트의 부모 아래에 런타임으로 생성됨.

- **turnDuration**: 한 턴이 유지되는 제한 시간(초). 이 시간이 지나면 자동으로 턴이 종료됨.

- **startDelay**: 전투 시작 후 첫 턴이 실제로 시작되기까지의 대기 시간(초).

### 캐릭터 매니저

캐릭터매니저는 배틀매니저의 부속품으로, 배틀매니저에서 받아온 characterData를 기반으로 적과 플레이어 양 측의 데이터를 저장함. 저장되는 data에는 덱과 최대 체력, 체력 등이 있으며, 그 외의 비주얼적 요소가 나타나지는 방식은 여기서 결정함.

### 에디터 조작 요소

- **startDeck**: 전투 시작 시 채워 넣을 초기 카드 목록. 실제 전투에서는 CharacterInit 호출 시 상위(BattleManager)에서 받아온 진짜 덱 데이터로 덮어써지므로, 인스펙터 값은 주로 단독 테스트용.

- **queueRoots**: 대기열(쿨타임 진행 중인 카드) 슬롯들의 부모 오브젝트. 그 자식 Transform 개수만큼 큐 슬롯으로 쓰임(현재 3칸).

- **isHandVisualized**: 손패 카드를 앞면(정보가 보이는 면)으로 표시할지 여부. 플레이어 쪽은 켜고 적 쪽은 꺼서 상대 패를 가리는 용도로 씀.

- **handsRoot**: 손패 슬롯들의 부모 오브젝트(현재 4칸).

- **hpBar**: 체력 비율에 맞춰 가로로 줄어드는 HP 바 Transform.

- **shrinkRight**: HP 바가 줄어들 때 오른쪽을 기준으로 줄어들지(true), 왼쪽을 기준으로 줄어들지(false) 결정. 플레이어/적 UI가 서로 마주보는 배치라 좌우 기준을 다르게 맞추기 위한 값.

- **playerControlled**: true면 입력(Input System)으로 카드를 직접 선택하고, false면 SelectRandomCard()로 무작위 카드를 고르는 AI 쪽으로 동작함.

- **costText**: 현재 코스트/최대 코스트를 표시하는 TextMeshPro.

- **effectListText**: 현재 걸려 있는 효과(버프·디버프) 목록을 이모지로 나열해 보여주는 TextMeshPro.

- **defenseText**: 현재 방어도 수치를 표시하는 TextMeshPro. 방어도가 1 이상일 때만 화면에 나타남.

# 아트 현황

현재 전투 진행 UI의 베타 아트가 완료되었고, 카드 일러스트의 베타 아트가 작업중에 있음. 현재로서 필요할 아트는

- 스타트 화면 및 게임 시작 버튼

- 적 일러스트

- 카드 효과 아이콘

- 이후 추가될 카드 일러스트

가 있음.

일러스트들의 자세한 그림체는 “아트 스타일 수정사항.md”파일 참고.
